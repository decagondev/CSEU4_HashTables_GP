
def my_hash(s):
    # turn the string in to some numbers
    pass

hash_table = [None] * 8 # lets start with size 8 as our clamping size

# take the hash ant turn it in to an index clamped to the size of a list